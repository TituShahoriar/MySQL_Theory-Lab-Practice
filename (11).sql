11.Explain the concept of subqueries in MySQL and provide an example.
A subquery is a query nested within another query. It allows you to use the result of one query as input to another query. Subqueries can be used in the SELECT, FROM, WHERE, and HAVING clauses.

Example:

sql

SELECT column_name FROM table_name WHERE column_name IN (